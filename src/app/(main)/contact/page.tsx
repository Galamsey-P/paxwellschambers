import { TransparentHeader } from "@/components/navigation";

export default function Contact() {
    return (
        <TransparentHeader>
            <main>

            </main>
        </TransparentHeader>
    );
}