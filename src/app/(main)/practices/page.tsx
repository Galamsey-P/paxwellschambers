import { TransparentHeader } from "@/components/navigation";

export default function Practices() {
    return (
        <TransparentHeader>
            <main>

            </main>
        </TransparentHeader>
    );
}